# -*- coding: utf-8 -*-
# vim: ft=yaml
---
arvados:
  pkg:
    name: arvados
  rootgroup: root
  config: '/etc/arvados'
  service:
    name: arvados
  subcomponent:
    config: '/etc/arvados-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
